<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".controller.MyVocab">

    <item android:id="@+id/del_my_vocab_button"
        android:icon="@drawable/ic_action_discard"
        android:orderInCategory="200"
        android:title="@string/del_vocab_button"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/label_my_vocab_button"
        android:icon="@drawable/ic_action_labels"
        android:orderInCategory="300"
        android:title="@string/label_vocab_button"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/select_all_my_vocab_button"
        android:icon="@drawable/ic_action_select_all"
        android:orderInCategory="400"
        android:title="@string/select_all_button"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/sort_my_vocab_button"
        android:icon="@drawable/ic_action_select_all"
        android:orderInCategory="500"
        android:title="@string/sort_vocab_button"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/search_my_vocab_button"
        android:icon="@drawable/ic_action_search"
        android:orderInCategory="150"
        android:title="@string/search_button"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom"/>


</menu>
